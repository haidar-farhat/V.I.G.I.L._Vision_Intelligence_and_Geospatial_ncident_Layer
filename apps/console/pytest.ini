[pytest]
testpaths = tests
# The engine, the console package, and the scene generator the engine's own
# tests use — reused here rather than duplicated.
pythonpath = . ../../engine ../../engine/tests
addopts = -q --strict-markers
